import { styled } from "@mui/material";
import { LoadingButton } from "@mui/lab";
export const StyledButtonNotFillLoad = styled(LoadingButton)({
  minWidth: '120px',
  padding: '2px 15px',
  marginRight: '10px',
  textTransform: 'none',
  fontSize: 13,
  borderRadius: '10px'
});